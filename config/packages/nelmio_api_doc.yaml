nelmio_api_doc:
    documentation:
        info:
            title: BileMo API
            description: L'API dédiée aux produits BileMo !
            version: 1.0.0
        tags:
            - name: "Users"
              description: "Manage Users"
            - name: "Products"
              description: "Manage Products"
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
